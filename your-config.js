// Insert your credentials and rename this file to config.js

var config = {};

config.credentials = {
    consumer_key: "<CONSUMER_KEY>",
    consumer_secret: "<CONSUMER_SECRET>",
    access_token: "<ACCESS_TOKEN>",
    access_secret: "<ACCESS_TOKEN_SECRET>"
};

module.exports = config;